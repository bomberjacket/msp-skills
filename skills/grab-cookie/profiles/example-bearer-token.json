{
  "_comment": "EXAMPLE profile: a site whose session is an opaque bearer token held in localStorage, sent as an Authorization header, sometimes with a second required header. Copy, rename, edit. Nothing here is specific to any vendor.",

  "name": "example-bearer-token",
  "site": "Example SaaS with an opaque localStorage token and a private API",

  "capture_hint": "Be logged into the site in Chrome. Press F12, open Network, click any request to the site's API, right-click it, choose Copy as cURL (bash), and paste into the capture file below.",
  "capture_file": "captures/example-bearer-token.curl.txt",

  "_extract_note": "Header extraction takes the header verbatim, so it does not matter whether the value is a JWT, an opaque string, or a vendor-specific format. A second header can be captured alongside it when the API requires one (tenant, realm, workspace, and so on).",
  "extract": [
    {
      "from": "header",
      "name": "authorization",
      "store_as": "EXAMPLE_AUTHORIZATION",
      "required": true
    },
    {
      "from": "header",
      "name": "x-tenant",
      "store_as": "EXAMPLE_TENANT",
      "required": false
    }
  ],

  "_wire_note": "env-file is REGENERATED from the credential store on every wire, so point `path` at a file this profile owns exclusively. A `lines` entry that carries a credential must be `KEY={PLACEHOLDER}`: a line whose whole content IS the secret (no `=`) cannot be recognised as this profile's own once the value rotates, and `wire` will ask you to delete the file each time. A static line with no placeholder (a comment, say) is fine - it is matched verbatim. Placeholder names are letters, digits and underscores, in either case, so a store_as copied from a mixed-case cookie name works. If the destination holds any setting this profile does not own, `wire` refuses rather than overwriting it - have your tool read this file and its own settings file separately.",
  "wire": {
    "type": "env-file",
    "path": "${HOME}/.config/example-cli/.env",
    "header_comment": "# Regenerated by the grab-cookie Skill. Keep this file out of version control.",
    "lines": [
      "EXAMPLE_AUTHORIZATION={EXAMPLE_AUTHORIZATION}",
      "EXAMPLE_TENANT={EXAMPLE_TENANT}"
    ],
    "mode": "0600"
  },

  "_verify_note": "expired_exit is for a command that signals a dead credential by exit code rather than by text. Use expired_on instead when the signal is in the output.",
  "verify": {
    "cmd": ["example-cli", "doctor"],
    "expired_exit": 2
  },

  "_ttl_note": "Opaque tokens often have no published lifetime. Leave ttl_days null and let the daily doctor detect expiry by probing.",
  "ttl_days": null,
  "warn_days": null
}
